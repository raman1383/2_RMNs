#include <stdio.h>
#include <stdbool.h>

int main()
{
    // data types
    int age = 32;
    bool isAllowed = false;

    // control flow
    if (age >= 18)
    {
        printf("drink !");
    }
    else if (isAllowed == false)
    {
        printf("Do not drink !!");
    }
    else
    {
        printf("Do not give a rats ass");
    }

    // loops

    return 0;
}

// functions